- [ ] Identify root cause of Vite/Babel error "Cannot use import statement outside a module".
- [ ] Update frontend entry/config so Vite serves `src/main.jsx` as ESM (fix `globals` import if needed).
- [ ] Re-run `frontend` dev server and confirm error disappears.
- [ ] If still failing, inspect `assets/styles/globals.js` and `vite.config.js` and apply required fix.

